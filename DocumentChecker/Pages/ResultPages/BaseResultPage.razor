@page "/result"
<PageTitle>ResultPage</PageTitle>

<div class="flex flex-col gap-6 py-8 ">
    <DocumentChecker.Components.MyTextBox Header=@Header Description=@TextResult Path="#" />
    @if (DataService.FoundErrors.Count > 0)
    {
        <DocumentChecker.Components.MyListBox Header="Nájdené chyby" Items="@DataService.FoundErrors" />        
    }
    <div class="flex-row gap-16 flex justify-between">
        <DocumentChecker.Components.MyButton ButtonContent="Ignoruj" Icon="" OnClick=OnIgnoreClick />
        <DocumentChecker.Components.MyButton ButtonContent="Oprav" Icon="" OnClick="OnCorrectClick" />
    </div>
</div>
