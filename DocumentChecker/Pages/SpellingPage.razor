@page "/spelling"
@inject NavigationManager NavigationManager
@inherits BaseJsPage;

<PageTitle>SpellingPage</PageTitle>
<div class="flex flex-col gap-6 py-8">
    <MyTextBox Header="Pravopis" Description="Kontrola vybraného pravopisného javu" Path="spelling" />
    <MyButton ButtonContent="Nahrajte pravidlo" Icon="fa-solid fa-file-import" OnClick="@OpenNewRulePage" />
    @if (SpellingPageDataService.Rules.Count > 0)
    {
        <DocumentChecker.Components.MyListBox Items="@SpellingPageDataService.Rules" Header="Vlastné pravidlá" />
    }
    <MyCheckbox Id="own-rules-check" Text="Kontrola vlastnými pravidlami" @bind-Checked="@SpellingPageDataService.CheckOwnRules" Description="Kontrola textu pomocou vlastných pravidiel." />
    <MyCheckbox Id="preposition-check" Text="Kontrola predložiek s/z, so/zo" @bind-Checked="@SpellingPageDataService.CheckPrepositions" Description="Kontrola správnosti použitia predložiek v texte." />
    <MyCheckbox Id="languagetool-check" Text="Kontrola pomocou LanguageTool" @bind-Checked="@SpellingPageDataService.CheckLanguageTool" Description="Kontrola textu pomocou nástroja LanguageTool." />
    <MyButton ButtonContent="ŠTART" Icon="fa-solid fa-play" Classes="w-48" OnClick="OnStartClick" />
</div>

